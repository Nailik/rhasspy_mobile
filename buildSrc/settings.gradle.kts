pluginManagement {
    plugins {
        id("de.fayard.refreshVersions") version "0.51.0"
////                                # available:"0.60.0"
    }
}

plugins {
    id("de.fayard.refreshVersions")
}